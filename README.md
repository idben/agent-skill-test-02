# 加法計算器

簡易網頁加法計算器，輸入兩個數字後顯示計算結果。

## 功能

- 輸入兩個數字進行加法運算
- 使用原生 HTML `<dialog>` 元素顯示結果
- 輸入驗證（非數字提示錯誤）

## 使用方式

直接用瀏覽器開啟 `index.html` 即可使用。

## 技術

- HTML5
- CSS3
- JavaScript（原生，無框架）

## 特色

採用 HTML5 原生 `<dialog>` 元素取代傳統 `window.alert()`，提供：
- 可自訂樣式的對話框
- 非阻塞式設計
- 支援鍵盤導航（Escape 關閉）
